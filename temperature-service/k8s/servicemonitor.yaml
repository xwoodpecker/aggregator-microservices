apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: temperature-service-monitor
  labels:
    release: my-prom
spec:
  selector:
    matchLabels:
      app: temperature-ms
  endpoints:
    - port: http